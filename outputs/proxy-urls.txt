hysteria://62.210.10.113:22662/?protocol=udp&insecure=NaN&peer=apple.com&auth=dongtaiwang.com&upmbps=11 Mbps&downmbps=55 Mbps&alpn=h3&obfs=#NL function getFlagEmoji(countryCode) {
  // 将国家/地区代码转换为大写，以匹配 ISO 3166-1 alpha-2 格式
  // const countryCodeUpper = countryCode.toUpperCase();

  // 计算国旗 emoji 的 Unicode 编码
  // 每个国家/地区的 Unicode 编码范围是从 'A' 到 'Z'，并且与国家/地区代码对应
  const unicodeOffset = 127397; // 国旗 emoji 的 Unicode 偏移量
  const firstLetter = countryCodeUpper.charCodeAt(0);
  const secondLetter = countryCodeUpper.charCodeAt(1);
  const emoji = String.fromCodePoint(firstLetter + unicodeOffset, secondLetter + unicodeOffset);
  return emoji;
}-hysteria | 0-1
tuic://083c4304-78e8-4702-b2ff-7dc899f74d90:dongtaiwang.com@62.210.10.113:22457/?congestion_controller=bbr&udp_relay_mode=native&sni=bing.com&alpn=h3&allow_insecure=NaN&disable_sni=0#NL function getFlagEmoji(countryCode) {
  // 将国家/地区代码转换为大写，以匹配 ISO 3166-1 alpha-2 格式
  // const countryCodeUpper = countryCode.toUpperCase();

  // 计算国旗 emoji 的 Unicode 编码
  // 每个国家/地区的 Unicode 编码范围是从 'A' 到 'Z'，并且与国家/地区代码对应
  const unicodeOffset = 127397; // 国旗 emoji 的 Unicode 偏移量
  const firstLetter = countryCodeUpper.charCodeAt(0);
  const secondLetter = countryCodeUpper.charCodeAt(1);
  const emoji = String.fromCodePoint(firstLetter + unicodeOffset, secondLetter + unicodeOffset);
  return emoji;
}-tuic | 3-1
hysteria://198.40.52.27:36194/?protocol=udp&insecure=NaN&peer=apple.com&auth=dongtaiwang.com&upmbps=11 Mbps&downmbps=55 Mbps&alpn=h3&obfs=#US function getFlagEmoji(countryCode) {
  // 将国家/地区代码转换为大写，以匹配 ISO 3166-1 alpha-2 格式
  // const countryCodeUpper = countryCode.toUpperCase();

  // 计算国旗 emoji 的 Unicode 编码
  // 每个国家/地区的 Unicode 编码范围是从 'A' 到 'Z'，并且与国家/地区代码对应
  const unicodeOffset = 127397; // 国旗 emoji 的 Unicode 偏移量
  const firstLetter = countryCodeUpper.charCodeAt(0);
  const secondLetter = countryCodeUpper.charCodeAt(1);
  const emoji = String.fromCodePoint(firstLetter + unicodeOffset, secondLetter + unicodeOffset);
  return emoji;
}-hysteria | 4-1
hysteria://195.154.200.40:2345/?protocol=udp&insecure=NaN&peer=bing.com&auth=dongtaiwang.com&upmbps=80&downmbps=100&alpn=h3&obfs=#FR function getFlagEmoji(countryCode) {
  // 将国家/地区代码转换为大写，以匹配 ISO 3166-1 alpha-2 格式
  // const countryCodeUpper = countryCode.toUpperCase();

  // 计算国旗 emoji 的 Unicode 编码
  // 每个国家/地区的 Unicode 编码范围是从 'A' 到 'Z'，并且与国家/地区代码对应
  const unicodeOffset = 127397; // 国旗 emoji 的 Unicode 偏移量
  const firstLetter = countryCodeUpper.charCodeAt(0);
  const secondLetter = countryCodeUpper.charCodeAt(1);
  const emoji = String.fromCodePoint(firstLetter + unicodeOffset, secondLetter + unicodeOffset);
  return emoji;
}-Hysteria | 0-0
hysteria://51.159.226.1:14241/?protocol=udp&insecure=NaN&peer=apple.com&auth=dongtaiwang.com&upmbps=80&downmbps=100&alpn=h3&obfs=#FR function getFlagEmoji(countryCode) {
  // 将国家/地区代码转换为大写，以匹配 ISO 3166-1 alpha-2 格式
  // const countryCodeUpper = countryCode.toUpperCase();

  // 计算国旗 emoji 的 Unicode 编码
  // 每个国家/地区的 Unicode 编码范围是从 'A' 到 'Z'，并且与国家/地区代码对应
  const unicodeOffset = 127397; // 国旗 emoji 的 Unicode 偏移量
  const firstLetter = countryCodeUpper.charCodeAt(0);
  const secondLetter = countryCodeUpper.charCodeAt(1);
  const emoji = String.fromCodePoint(firstLetter + unicodeOffset, secondLetter + unicodeOffset);
  return emoji;
}-Hysteria | 1-0
hysteria2://dongtaiwang.com@51.159.111.32:31180/?sni=apple.com&insecure=NaN#FR function getFlagEmoji(countryCode) {
  // 将国家/地区代码转换为大写，以匹配 ISO 3166-1 alpha-2 格式
  // const countryCodeUpper = countryCode.toUpperCase();

  // 计算国旗 emoji 的 Unicode 编码
  // 每个国家/地区的 Unicode 编码范围是从 'A' 到 'Z'，并且与国家/地区代码对应
  const unicodeOffset = 127397; // 国旗 emoji 的 Unicode 偏移量
  const firstLetter = countryCodeUpper.charCodeAt(0);
  const secondLetter = countryCodeUpper.charCodeAt(1);
  const emoji = String.fromCodePoint(firstLetter + unicodeOffset, secondLetter + unicodeOffset);
  return emoji;
}-Hysteria2 | 0-0
hysteria2://dongtaiwang.com@108.181.5.130:57773/?sni=apple.com&insecure=NaN#US function getFlagEmoji(countryCode) {
  // 将国家/地区代码转换为大写，以匹配 ISO 3166-1 alpha-2 格式
  // const countryCodeUpper = countryCode.toUpperCase();

  // 计算国旗 emoji 的 Unicode 编码
  // 每个国家/地区的 Unicode 编码范围是从 'A' 到 'Z'，并且与国家/地区代码对应
  const unicodeOffset = 127397; // 国旗 emoji 的 Unicode 偏移量
  const firstLetter = countryCodeUpper.charCodeAt(0);
  const secondLetter = countryCodeUpper.charCodeAt(1);
  const emoji = String.fromCodePoint(firstLetter + unicodeOffset, secondLetter + unicodeOffset);
  return emoji;
}-Hysteria2 | 1-0
vless://ec302de4-6b3d-4f03-a228-3c6d303ed370@1.162.51.106:37898?encryption=none&flow=xtls-rprx-vision&security=reality&sni=www.yahoo.com&fp=chrome&pbk=v6uWJJmVGFPd0K040QK3PJHCbPc74kXFoQmAhpu30jQ&sid=e12d7fdb&type=tcp&serviceName=/&host=&path=#TW function getFlagEmoji(countryCode) {
  // 将国家/地区代码转换为大写，以匹配 ISO 3166-1 alpha-2 格式
  // const countryCodeUpper = countryCode.toUpperCase();

  // 计算国旗 emoji 的 Unicode 编码
  // 每个国家/地区的 Unicode 编码范围是从 'A' 到 'Z'，并且与国家/地区代码对应
  const unicodeOffset = 127397; // 国旗 emoji 的 Unicode 偏移量
  const firstLetter = countryCodeUpper.charCodeAt(0);
  const secondLetter = countryCodeUpper.charCodeAt(1);
  const emoji = String.fromCodePoint(firstLetter + unicodeOffset, secondLetter + unicodeOffset);
  return emoji;
}-vless | 0-0